# ETHEREUM dataset from zenodo

## source
https://zenodo.org/records/4718440

## directions 
Uncompress them using `bunzip2`
run `combine_txn.sh` to combine transactions into a single file `ethereum_txs.txt`
run `combine_time.sh` to combine block timestamps into a single file `ethereum_blocktime.txt`